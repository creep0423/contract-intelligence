# Scenario compatibility fixtures

该目录仅保存历史场景解析所需的最小 `scenario.toml` 和 `faq.csv`。

这些文件用于验证通用场景配置、source 归一化、意图分类和兼容性测试，不属于
Contract Intelligence 产品运行时数据，也不得被部署脚本作为知识库源导入。

产品运行时配置只位于 `scenarios/tender_contract_risk/`。
